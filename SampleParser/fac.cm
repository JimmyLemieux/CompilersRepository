/* A program to compute the factorial value of an input */

void main(void) {
  int x; 
  int fac;
  int a[100];
  a[2] = 4;
  x = input();

  while (x > 1) {
    fac = fac * x;
    x = x - 1;
  } 

  output(fac);
}